import React from 'react';
import PropTypes from 'prop-types';

import Navbar from '../../components/Header/Navbar'

Home.propTypes = {
  
};

function Home(props) {
  return (
    <div>
      <Navbar />
    </div>
  );
}

export default Home;